# Feature pages transcription report (agent B: sms-payment-link / crm-with-invoicing / all-in-one-inbox / lead-management)
- tsc clean; all copy verbatim with typos preserved (H1 "Link:Get", "...billing software? I", keyword typos); zero image substitutions.
- sms-payment-link: 11 sections. Live table is 5-column; schema is 3-column → paired columns joined with " · " (all cell strings verbatim). One inferred cell ("Mailed paper check" → Logged to CRM = "No"). 3 secondary platform-link buttons dropped (split supports one link; kept "Missed Call Text Back").
- crm-with-invoicing: 9 sections; compare 8 rows; one inferred cell ("Workflow fires after payment" → "✅ Automatic"); rejoined one extraction-split sentence.
- all-in-one-inbox: 11 sections incl. 10-channel title-only grid (body:"" on those steps) + setupSteps; compare 7 rows complete; one secondary link dropped; faqContextLabel verbatim reuses the mctb alt (source quirk).
- lead-management-software-for-small-business: 9 sections; compare 8 rows complete; no inferred cells.
