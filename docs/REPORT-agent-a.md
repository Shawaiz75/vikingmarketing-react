# Feature pages transcription report (agent A: review-generation-software / website-chat-widget / database-reactivation)
- All copy verbatim (scripted audit against outlines); tsc clean; zero image substitutions; zero inferred table cells.
- review-generation-software: definition → split(why reviews) → iconSteps×5 → iconBullets×8 → split(platform) → split(no gating) → industryCards×6 → local → reviews. Platform split supports one link → kept "Missed Call Text Back", dropped extra "AI Appointment Setter"/"All in One Inbox" buttons (copy still names every feature).
- website-chat-widget: definition → split(why) → iconSteps×3 (three ways to chat) → iconSteps×5 (how it works) → iconBullets×9 → split(one platform) → industryCards×6 → local → reviews. Dropped 3 extra platform-links (kept MCTB).
- database-reactivation: definition → split(old contacts) → iconSteps×5 → iconBullets×8 → compare(7 complete rows; col order verbatim Factor/DBR/Buying New Leads) → split(compliance) → industryCards×6 → local → reviews.
- faqContextLabel quirks preserved from source (review page reuses "the Missed call text back software").
