# Industry pages transcription report (agent E: saas / healthcare / agencies / b2b-sales)
- All 4 pages map: hero → definition → split(left) why → iconSteps(5) → split(right) unique → compare (6 rows, footnote, outro; no inferred cells) → split(left) More Than a Booking Tool (bullets + bold + See the full platform → /) → reviews → industryLinks.
- schema kinds [localBusiness, person, service, webPage, howTo, faq]; howTo from iconSteps.
- Images all resolved from the manifest, no substitutions; step-5 icon uses hashed path (no friendly alias).
- Typos preserved verbatim (e.g. "Viking AI:The lead hears back fast.").
- FAQ counts: SaaS 12, Healthcare 14, Agencies 15, B2B 12.
